services:
  - type: web
      name: ampleleads-mcp
          env: python
              buildCommand: pip install -r requirements.txt
                  startCommand: python server.py
                      envVars:
                            - key: AMPLE_LEADS_API_KEY
                                    sync: false
